{
  "name": "final-vote",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp site/index.html dist/index.html && sh -c 'cp site/*.png site/*.PNG site/*.jpg dist/ 2>/dev/null; true'",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^11.10.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "playwright-core": "^1.62.1",
    "vite": "^6.3.5"
  }
}
